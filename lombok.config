# Lombok Configuration
# Make warnings more prominent in IDE

# Enable equals/hashCode warnings for inheritance
lombok.equalsAndHashCode.callSuper = call

# Show all Lombok warnings
lombok.log.level = WARNING

# Make SuperBuilder warnings more visible
config.stopBubbling = true